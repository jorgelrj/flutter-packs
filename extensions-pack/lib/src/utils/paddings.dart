import 'package:extensions_pack/extensions_pack.dart';
import 'package:flutter/material.dart';

const kXXSAll = EdgeInsets.all(kXXSSize); // 4
const kXXSVertical = EdgeInsets.symmetric(vertical: kXXSSize);
const kXXSHorizontal = EdgeInsets.symmetric(horizontal: kXXSSize);
const kXXSBottom = EdgeInsets.only(bottom: kXXSSize);
const kXXSTop = EdgeInsets.only(top: kXXSSize);
const kXXSRight = EdgeInsets.only(right: kXXSSize);
const kXXSLeft = EdgeInsets.only(left: kXXSSize);

const kSAll = EdgeInsets.all(kGridSpacer); // 8
const kSVertical = EdgeInsets.symmetric(vertical: kGridSpacer);
const kSHorizontal = EdgeInsets.symmetric(horizontal: kGridSpacer);
const kSBottom = EdgeInsets.only(bottom: kGridSpacer);
const kSTop = EdgeInsets.only(top: kGridSpacer);
const kSRight = EdgeInsets.only(right: kGridSpacer);
const kSLeft = EdgeInsets.only(left: kGridSpacer);

const kXSAll = EdgeInsets.all(kXSSize); // 16
const kXSVertical = EdgeInsets.symmetric(vertical: kXSSize);
const kXSHorizontal = EdgeInsets.symmetric(horizontal: kXSSize);
const kXSBottom = EdgeInsets.only(bottom: kXSSize);
const kXSTop = EdgeInsets.only(top: kXSSize);
const kXSRight = EdgeInsets.only(right: kXSSize);
const kXSLeft = EdgeInsets.only(left: kXSSize);

const kSMAll = EdgeInsets.all(kSMSize); //24
const kSMVertical = EdgeInsets.symmetric(vertical: kSMSize);
const kSMHorizontal = EdgeInsets.symmetric(horizontal: kSMSize);
const kSMBottom = EdgeInsets.only(bottom: kSMSize);
const kSMTop = EdgeInsets.only(top: kSMSize);
const kSMRight = EdgeInsets.only(right: kSMSize);
const kSMLeft = EdgeInsets.only(left: kSMSize);

const kLGAll = EdgeInsets.all(kLGSize); // 40
const kLGVertical = EdgeInsets.symmetric(vertical: kLGSize);
const kLGHorizontal = EdgeInsets.symmetric(horizontal: kLGSize);
const kLGBottom = EdgeInsets.only(bottom: kLGSize);
const kLGTop = EdgeInsets.only(top: kLGSize);
const kLGRight = EdgeInsets.only(right: kLGSize);
const kLGLeft = EdgeInsets.only(left: kLGSize);

const kXLAll = EdgeInsets.all(kXLSize); // 48
const kXLVertical = EdgeInsets.symmetric(vertical: kXLSize);
const kXLHorizontal = EdgeInsets.symmetric(horizontal: kXLSize);
const kXLBottom = EdgeInsets.only(bottom: kXLSize);
const kXLTop = EdgeInsets.only(top: kXLSize);
const kXLRight = EdgeInsets.only(right: kXLSize);
const kXLLeft = EdgeInsets.only(left: kXLSize);

const kXXLAll = EdgeInsets.all(kXXLSize); // 64
const kXXLVertical = EdgeInsets.symmetric(vertical: kXXLSize);
const kXXLHorizontal = EdgeInsets.symmetric(horizontal: kXXLSize);
const kXXLBottom = EdgeInsets.only(bottom: kXXLSize);
const kXXLTop = EdgeInsets.only(top: kXXLSize);
const kXXLRight = EdgeInsets.only(right: kXXLSize);
const kXXLLeft = EdgeInsets.only(left: kXXLSize);
